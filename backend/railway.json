{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "preDeployCommand": "python -m scripts.release",
    "restartPolicyType": "ON_FAILURE"
  }
}
